name: Examlpe Test Workflow

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Building
        run: echo Building...
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Testing
        run: echo Testing...
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploying
        run: echo Deploying...